# scripts/

Bootstrap scripts for initializing a repo created from `calvindotsg/template-python-cli`.

See [TEMPLATE.md](../TEMPLATE.md) for the full setup guide.

**Note:** The entire `scripts/` directory is removed by `init.sh` as its final step. Consumer repos will not retain this directory after initialization.
